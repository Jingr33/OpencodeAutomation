---
description: List repositories registered in the framework workspace
subtask: true
---

Run `python .opencode/scripts/repository.py list` and report each repository's
path, remote, and current branch. Include repositories found under the configured
repository root even when their registry entry is missing.
