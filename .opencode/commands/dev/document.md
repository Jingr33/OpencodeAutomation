---
description: Create or update active repository documentation
agent: docs
subtask: true
---

Document or update `$ARGUMENTS` in the active repository. Inspect its existing
documentation structure first. If the argument is `opencode`, document the
shared `.opencode` setup; if it is `all`, review only stale pages. Do not create
project architecture claims without inspecting the project.
