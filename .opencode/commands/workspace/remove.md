---
description: Remove a folder from the VS Code multi-root workspace
subtask: true
---

Run:

```bash
python .opencode/scripts/workspace.py remove <path-or-name>
```

This removes a worktree or external repository entry from the workspace file.
Use this when a worktree is cleaned up or the task is complete.
