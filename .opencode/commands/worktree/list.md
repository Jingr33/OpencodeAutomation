---
description: List all worktrees for a repository with branch and dirty status
subtask: true
---

Run `python .opencode/scripts/worktree.py list [--repo <path>]`. Include the main
worktree, paths, branches, detached states, and whether each worktree is dirty.
